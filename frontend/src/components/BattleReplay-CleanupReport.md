# Battle Replay UI Cleanup - Шаг 1 Выполнен

## Выполненные изменения

### 1. ✅ Убраны координаты с клеток сетки
- Координаты (0,0, 1,0...) теперь скрыты по умолчанию
- Добавлен state `showDebugInfo` для управления отображением
- Координаты показываются только когда `showDebugInfo === true`

### 2. ✅ Добавлена кнопка Debug Mode
- Кнопка "🐛 Debug" в header справа
- Переключает отображение координат
- Визуальная индикация активного состояния (жёлтый фон)

### 3. ✅ Убран заголовок "⚔️ Повтор боя"
- Удалён дублирующий h1 заголовок
- Header теперь более компактный

### 4. ✅ Убран label "Поле боя (8×10)"
- Удалён h3 над сеткой
- Очевидно из контекста что это поле боя

### 5. ✅ Упрощён header
**Было:**
```
⚔️ Повтор боя                    [← Назад]
Игрок 1: Name | Игрок 2: Name | Победитель: Name | Статус: completed
```

**Стало:**
```
Name vs Name                     [🐛 Debug] [← Назад]
Победитель: Name
```

- Убраны labels "Игрок 1:", "Игрок 2:"
- Убрано поле "Статус" (техническая информация)
- Имена игроков с цветовым кодированием (синий/красный)
- Более компактный и читаемый layout

## Технические детали

### Изменённые файлы
- `frontend/src/components/BattleReplay.tsx`

### Добавленный state
```typescript
const [showDebugInfo, setShowDebugInfo] = useState(false);
```

### Обновлённые компоненты
1. **ReplayGridCell** - добавлен prop `showDebugInfo`
2. **Header** - упрощён layout, добавлена кнопка Debug
3. **Grid section** - убран label

### Зависимости
- Добавлен `showDebugInfo` в dependencies массив `useMemo` для grid

## Результат

### До
- Визуальный шум от координат на каждой клетке
- Дублирование информации в header
- Избыточные labels

### После
- Чистое поле боя без координат
- Компактный header с ключевой информацией
- Опциональный debug mode для разработчиков

## Время выполнения
~30 минут (как и планировалось)

## Следующий шаг
Шаг 2: Индикатор активного юнита (1 час)
