import { Module } from '@nestjs/common';

/**
 * Configuration module for game constants and settings.
 * Provides centralized access to all game configuration values.
 */
@Module({
  providers: [],
  exports: [],
})
export class ConfigModule {}